# Flask 환경 설정
FLASK_ENV=production
SECRET_KEY=your-secret-key-here

# MySQL RDS 데이터베이스 설정
DATABASE_URL=mysql+pymysql://username:password@your-rds-endpoint:3306/database_name

# 예시:
# DATABASE_URL=mysql+pymysql://admin:yourpassword@clubu-db.xxxxxxxxx.ap-northeast-2.rds.amazonaws.com:3306/clubu_db

# 파일 저장 경로 설정 (Docker 컨테이너에서 사용)
# 로컬 개발환경에서는 기본값(banners, clubs, notices) 사용
# 프로덕션 환경에서는 /data/banners, /data/clubs, /data/notices 사용
BANNERS_DIR=/data/banners
CLUBS_DIR=/data/clubs
NOTICES_DIR=/data/notices